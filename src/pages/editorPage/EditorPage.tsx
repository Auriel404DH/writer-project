import React from 'react';
import styles from './EditorPage.module.scss';

const Editor: React.FC = () => {
  return (
    <div className={styles.editor}>
      <div className={styles.editor__title}>Editor</div>
    </div>
  );
};

export default Editor;
